{
	"name": "compiler",
	"private": "true",
	"version": "1.0.0-prealpha1",
	"scripts": {
		"compile:win": "rmdir /s /q \"dist\" && tsc",
		"compile:darwin": "clear && rm -rf dist && tsc",
		"compile:actions": "tsc",
		"test": "jest"
	},
	"engines": {
		"node": ">=16.16.0"
	},
	"dependencies": {
		"@types/node": "^18.7.14",
		"nodemon": "^2.0.19"
	},
	"devDependencies": {
		"jest": "^29.0.3"
	}
}
